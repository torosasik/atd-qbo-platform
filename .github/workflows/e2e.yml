name: E2E & Visual Regression

# Disabled until emulator and test infrastructure is set up
on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: E2E tests not yet configured
        run: echo "E2E tests are disabled until emulator setup is complete."
